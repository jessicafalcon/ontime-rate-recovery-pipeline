# GENERATED by scripts/gen_dbt_sources.py from generator/models.py — do not edit; run `make gen-sources`.
version: 2

sources:
  - name: raw
    schema: raw
    description: Raw landing — the Amplitude export shape and the dim seed file, loaded by `make load` (DuckDB) / `make bq-load` (BigQuery). No freshness config (it reads the clock).
    tables:
      - name: events
        description: One row of the Amplitude raw export.
        columns:
          - name: insert_id
            description: varchar
            data_tests:
              - not_null
          - name: event_type
            description: varchar
            data_tests:
              - not_null
              - accepted_values:
                  arguments:
                    values: ["prompt_sent", "prompt_delivered", "prompt_opened", "capture_started", "upload_started", "upload_failed", "upload_completed", "response_recorded", "app_opened"]
          - name: user_id
            description: varchar
            data_tests:
              - not_null
          - name: device_id
            description: varchar
            data_tests:
              - not_null
          - name: client_event_time
            description: timestamp
            data_tests:
              - not_null
          - name: server_received_time
            description: timestamp
            data_tests:
              - not_null
          - name: server_upload_time
            description: timestamp
            data_tests:
              - not_null
          - name: event_properties
            description: json
            data_tests:
              - not_null
      - name: dim_user
        description: One SCD2 row of `dim_user` (§2.3). `valid_to` None = current row.
        identifier: "{{ var('dim_user_identifier', 'dim_user') }}"
        columns:
          - name: user_id
            description: varchar
            data_tests:
              - not_null
          - name: tz
            description: varchar
            data_tests:
              - not_null
          - name: cohort_id
            description: varchar
            data_tests:
              - not_null
          - name: signup_date
            description: date
            data_tests:
              - not_null
          - name: valid_from
            description: timestamp
            data_tests:
              - not_null
          - name: valid_to
            description: timestamp, nullable
