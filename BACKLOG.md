# BACKLOG.md — deferred findings and revisits

Items accepted "for now" with a concrete revisit trigger. Reviewed at every
phase exit and, since the Phase 13 close, at every `fix/` branch exit (alongside
the coherence audit); an item whose trigger has arrived is either done in that
branch or re-deferred here with a new trigger — never
silently dropped. Cite rows by TITLE (bold text); line numbers shift. A closed
row is struck through with "DONE Phase N" — or the landing branch, e.g.
"DONE — `fix/<slug>`", when a non-phase fix closes it — never deleted.

| Item | Source | Trigger |
|---|---|---|
| ~~**Mutation sweep has no operator for dbt SQL** — an invariant upheld only in SQL is pinned by a dbt unit/data test named in the spec's Invariants table, not by `make mutate`. Candidate: a `swap-predicate` / `drop-where` operator over a model file, run through `dbt build` in the worktree. Re-deferred at Phase 2: staging's one SQL invariant (dedupe) is pinned by a unit test, a `unique` test and a Python pin, and both SQL clauses were shown to turn a unit test red by hand; attribution's five-arm `case` is the first place a survivor could hide.~~ **DONE Phase 3** — `drop-arm:<n>` / `swap-arms:<i>,<j>` over one `case … end as <alias>` in `dbt/models/**.sql`, killed through the suite's in-process `dbt build`; nine lines in the Phase 3 spec. Operators beyond `case` arms: a new row when a survivor class needs one. | Phase 0; re-deferred Phase 2 | Phase 3 (attribution precedence is the first multi-branch SQL) |
| ~~**Phase 0 has no golden fixture** — `make test` runs offline but the frozen `fixtures/tiny/` that turns correctness into a diff is Phase 1's deliverable. Until it lands, the review gate proves tooling, not pipeline.~~ **DONE Phase 1** — `fixtures/tiny/MANIFEST.sha256` committed; gate check f. | Phase 0 | Phase 1 (resolved when `MANIFEST.sha256` is committed) |
| ~~**Raw DDL and dbt `sources.yml` are not yet generated from `generator/models.py`** — the schema contract says generated-never-hand-edited; the generator side exists, the consumer does not.~~ **DONE Phase 2** — `scripts/gen_dbt_sources.py` (`make gen-sources`) renders `loader/ddl.sql` + `dbt/models/staging/sources.yml`; `tests/test_dbt_sources.py` fails on a hand edit. | Phase 1 | Phase 2 (first dbt source) |
| **Spanner bills from creation (~$0.09/h, ~$65/mo) — never leave the instance up** — `enable_spanner` toggle + a teardown date in `docs/DEPLOYMENT.md`. *Re-checked Phase 9a exit: the `spanner` module is written behind `enable_spanner=false` (count-gated), not applied, ~~so the trial clock has not started~~ (no such clock — Amendment M); `docs/DEPLOYMENT.md` carries the teardown-date placeholder; re-deferred unchanged.* *Phase 9b revisit: no apply of the module; unchanged.* *Phase 10: the module gained its body (instance/database/federation) — still count-gated, still not applied; the runbook now pins the apply-day date lines and the same-day `VARS='enable_spanner=false'` teardown; ~~the clock has not started~~ (no such clock — M).* *Phase 10 apply day 2026-08-30: applied 23:37 UTC ~~(the trial clock STARTED — ends 2026-11-28)~~, `test-int-spanner` 4 passed, torn down 23:50 UTC the same session (`8 destroyed`, `Listed 0 items.`); the dated lines are in `docs/DEPLOYMENT.md`. Nothing billable is up; ~~every later `enable_spanner=true` apply (Phase 12 demo) is inside the same 90-day window or pays ~$65/mo~~ every later apply bills ~$0.09/h from creation — the row stays open as the reminder.* *2026-08-31: re-applied 02:30 UTC (Amendments E–F's live proof — `4 passed` under the custom role), torn down 02:48 UTC (`9 destroyed`, `Listed 0 items.`); ~18 minutes up.* *2026-08-31, Amendment M: the instance is `PROVISIONED` — a free-trial instance is a separate, console-created kind — so there was never a trial clock; the title and trigger corrected, the dated "trial ends" lines struck in DEPLOYMENT.* *2026-08-31 third session (round 4, Amendment N3's live re-proof): applied 06:07 UTC, torn down 06:42 UTC (`9 destroyed`, `Listed 0 items.`); ~35 min ≈ 5¢.* *Phase 12 entry (2026-09-01): re-confirmed clean before any apply — `gcloud spanner instances list` → `Listed 0 items.`, Composer API `SERVICE_DISABLED`; row stays open (the standing every-exit reminder).* *Phase 12 demo day (2026-09-01): applied 02:42 UTC for the rehearsal, torn down 03:32–03:41 UTC the same session in the combined Composer+Spanner toggle-flip (`14 destroyed`), `Listed 0 items.` at exit; ~50 min up ≈ 8¢. Row stays open.* *Phase 13 exit (docs-only, no apply): re-confirmed clean at the standing every-exit check — `gcloud spanner instances list` → `Listed 0 items.`; row stays open. fix/roadmap exit 2026-09-01 (records + one guard edit, no apply): `Listed 0 items.` again — the first `fix/`-exit run of the re-anchored trigger.* *fix/public-release exit (2026-09-01, the pre-publication review; records + one guard, no apply): `gcloud spanner instances list` → `Listed 0 items.` again.* *fix/front-door exit (2026-09-01, README only, no apply): the same-day read-only `gcloud spanner instances list` → `Listed 0 items.` stands for this exit too.* *fix/process-doc exit (2026-09-01, docs + one test, no apply): `gcloud spanner instances list` → `Listed 0 items.`; row stays open.* | Architecture review 2026-08-24 | Every phase or `fix/` branch exit: `gcloud spanner instances list` → `Listed 0 items.`; every apply session ends with the toggle-flip teardown |
| **Terraform state is a local, unversioned `infra/terraform.tfstate`** — the GCS backend is commented out in `infra/main.tf` (bootstrap-documented only), so losing the working copy after an `enable_spanner=true` apply leaves a billing 100-PU instance with no `tf-destroy`/toggle-flip path (a hand reconcile: `gcloud spanner instances delete`, the custom role, the connection); the file also holds every resource attribute in cleartext — project id, SA email, connection config — unencrypted and unversioned on one laptop, so the migration is a confidentiality fix as much as a cost one (round 4, security-reviewer #7) — that content is METADATA: no key, password or token resource exists in this stack, so nothing in the file is a credential (round 5 #12). Accepted for now: every Spanner apply is torn down in the same session (the row above). *Re-deferred 2026-08-31 at round 4's live re-proof (developer's call): the literal trigger fired for a 35-minute apply-prove-teardown session, which is exactly what the acceptance rationale covers; trigger reworded.* *Phase 12 entry: RE-DEFERRED (developer's call). Cost half NOT triggered — every Phase 12 apply is torn down in the same session (the demo is not a persistent stack). Confidentiality half deferred — a GCS remote backend (`init -migrate-state`) introduced immediately before the highest-stakes live apply/destroy of the project adds a failure mode to the exact teardown that must be reliable, and the file is metadata, not credentials (round 5 #12: no key/token/password resource). New trigger: the repo goes public, the project is reused, or a stack is meant to persist beyond one session.* *Phase 13 exit (the docs/narrative capstone that makes the repo presentation-facing): DUE for the confidentiality half — named as its own branch `fix/tf-remote-state`, NOT built in a docs-only phase (migrating state mid-docs-phase is unearned scope and out of Phase 13's Scope). The migration is `docs/DEPLOYMENT.md` § state-backend bootstrap + `init -migrate-state` on `<project_id>-tfstate`.* *fix/public-release exit (2026-09-01 — the "repo goes public" trigger has arrived): still local, still gitignored, never in history (`git rev-list --all` name scan); the file holds the project id and SA email the records now redact, so the confidentiality half is due in `fix/tf-remote-state` (ROADMAP item 2) BEFORE the visibility flip, or accepted there in DECISIONS — re-deferred in writing, not silent.* *2026-09-01, later: the developer flipped the repository public BEFORE `fix/tf-remote-state`, by decision — the state file is local and gitignored and was never in any ref, so the flip exposed nothing; the confidentiality half is now due at the next `tf-apply` session, before any state carrying live resources outlives that session.* | Review round 3, Phase 10 (security-reviewer #4) | Before the first `enable_spanner=true` apply that is NOT torn down in the same session (the Phase 12 demo), and for the confidentiality half BEFORE the repository is made public (`fix/tf-remote-state`, ROADMAP item 2 — the flip follows it): bootstrap the versioned `<project_id>-tfstate` bucket and migrate state (`docs/DEPLOYMENT.md` § bootstrap) |
| ~~**Budget alerts do not stop spend** — optional Pub/Sub → Cloud Function that disables billing at $150 is the real guardrail; documented as optional in Phase 9, built only if the author wants it.~~ **DONE Phase 9a** — `docs/DEPLOYMENT.md` documents the Pub/Sub → Cloud Function billing kill-switch as the optional real guardrail (with the resource sketch); not built (nothing billable runs by default — the `budget` module sets 50/150-in-account-currency alerts only). Build before a long-lived apply (Phase 12) if a hard stop is wanted. *Phase 12 (developer's call): NOT built. The hard stop guards a LONG-LIVED apply; Phase 12 is same-session apply-and-teardown with ask-first confirmation at every step, so the disciplined teardown + the `Listed 0 items.` / empty-Composer-list exit check is the guardrail. New trigger: a Composer/Spanner apply intended to persist beyond one session.* | Architecture review 2026-08-24 | Phase 9 |
| ~~**CI green on the Phase 0 PR is unverified until first push** — Done-when 6 / Evidence row 6; the workflow has never run (branch unpushed).~~ **DONE Phase 0** — `ci / lint-test` green on PR #1 (run 32880728851). | Review round 1 | First push of `phase-0-skeleton`: confirm `ci / lint-test` green, then strike |
| ~~**`partition_overwrite` has a DuckDB body no test renders and no model calls** — it only deletes (the name promises delete-and-insert) and joins `partitions` unquoted; it is the seam Phase 7's incremental models will call.~~ **DONE Phase 7** — completed to delete-and-insert in the set-based subquery form (no partition list to mis-quote), wired as the custom incremental strategy `get_incremental_partition_overwrite_sql` on the three event-level models, rendered by `tests/test_dbt_conventions.py::test_partition_overwrite_renders_delete_and_insert_on_duckdb` and executed by the two-landing build. Still five dispatch macros; BigQuery raises until Phase 9. | Review round 1, Phase 2 | Phase 7 (first caller; render test + insert half land with it) |
| ~~**`stg_prompts` carries two cohort keys** — `cohort_id` (dim_user) and `prompt_cohort_id` (the prompt_sent event) with no relationship test; the cohort-day denominator needs one.~~ **DONE Phase 3** — `attribution.cohort_id` is the prompt's own; `dbt/tests/assert_prompt_cohort_matches_dim.sql` pins equality. | Review round 1, Phase 2 | Phase 3 (attribution picks the key; a `relationships`/equality test lands with it) |
| ~~**Staging pins are counts only** — a content regression on the real fixture (e.g. keeping the later duplicate) is caught by the synthetic unit tests, not by a row-hash of `stg_events`/`stg_prompts` on tiny.~~ **DONE Phase 3** — `fixtures/tiny/expected/attribution.csv` pins every label (a function of the staged columns that matter); no staging row-hash — revisit at Phase 4 if a mart reads a staged column no label does. *Phase 4 revisit: `ontime_retention` reads `stg_events.client_event_time_local` on `app_opened` (no label does); pinned by `tests/pins.py::ORGANIC_OPEN_ROWS` (211), not a row-hash — the retention mart is not frozen (see its own row).* | Review round 1, Phase 2 | Phase 3 (`fixtures/tiny/expected/` is the content pin; add a staging row-hash there or here) |
| ~~**The BigQuery landing has no conflicting-duplicate guard** — `landing/load.py::conflicting_duplicates` refuses a landing where one `insert_id` + clock triple carries more than one payload (invariant 1, amendment 3); the Amplitude → BigQuery export path (Phase 9) runs the same `qualify` dedupe with no equivalent check, so such a row would be silently resolved there. Unreachable today (the generator cannot emit one). *Phase 9a revisit: no BigQuery build ran (9a is Terraform only); re-deferred to 9b's first `TARGET=bigquery` build unchanged.*~~ **DONE Phase 9b** — `dbt/tests/assert_no_conflicting_duplicates.sql`: the loader's predicate restated over `source('raw', 'events')`, payload compared key by key through `json_extract` (a JSON column is neither groupable nor castable on BigQuery — §8), so BOTH dialects fail the build on one; `tests/test_staging.py::test_conflicting_duplicate_fails_the_dbt_test` plants one. | Review round 3, Phase 2 | Phase 9b (first `make dbt-build TARGET=bigquery`: a source data test or a landing assertion with the same predicate) |
| ~~**The denominator contract's sum must exclude `delivery_fault`** — the five labels partition every `stg_prompts` row and `delivery_fault` is by definition undelivered, so `sum(all five) = prompts_delivered + delivery_fault`; CLAUDE.md's `sum(label counts) == prompts_delivered` holds only over the four delivered labels. `attribution` exposes `delivered_in_grace` for the mart.~~ **DONE Phase 4** — `dbt/tests/assert_cohort_day_partition.sql` pins both identities; `docs/METRICS.md` § `prompts_delivered`; CLAUDE.md and PHASES corrected (wording, DECISIONS Phase 4). | Phase 3 exit audit | Phase 4 (`docs/METRICS.md` defines the denominator; the cohort-day dbt test sums the four delivered labels) |
| **Every golden's sort key is hard-coded to its first two columns** — `eval/golden.py::export_rows` sorts by `(r[0], r[1])` while `Golden.key_width` (the diff key: 1 for attribution, 2 for the daily mart) is a separate field; both current specs declare a two-column sort key so it holds, but a future `Golden` with a wider key would sort by a prefix. Deriving the sort from `key_width` would cost the `swap-sort-key` mutation (the operator needs a literal tuple). *Phase 5 revisit: the third `Golden` (`scores_send_time`) has `key_width` 1 and sorts by `(user_id, cohort_id)` — not pulled.* | Review round 1, Phase 4 | A `Golden` with `key_width > 2`: add a `sort_width` field and its own mutation line |
| ~~**A user whose `tz` changes mid-window straddles two local clocks** — `u-000008` and `u-000010` switch Asia/Tokyo → Europe/London inside tiny (SCD2 `valid_from`); `stg_events.client_event_time_local` is correct per event, but a per-user organic-open histogram that pools both rows pulls the reachable-window estimate toward a zone the user has left. Phase 4's marts are unaffected (grain is cohort × local date; each prompt carries its own local time).~~ **DONE Phase 5** — per `user_id`, pooled on each event's own local hour: the generator draws one latent centre per user in LOCAL hours and converts through that day's tz, so both halves are samples of one clock and pooling in local time is the correct estimate (pooling in UTC would be the bug); unit test `features_user_hour_pools_a_tz_change`, DECISIONS Phase 5. | Phase 4 exit audit | Phase 5 reconciliation: decide whether `features` aggregate per user or per user-tz row before the model is written; a unit test with one user across two `tz` rows lands with it |
| **The cohort-window argmax and its tie-break are an `order by` in a window function** — `scores_send_time.sql`'s `row_number() over (… order by mass desc, hour_local asc)` is outside every mutation operator (Python: four; SQL: `case` arms), so invariant 7 (ties → the smaller hour) is pinned only by the unit test `scores_send_time_breaks_a_window_tie_by_smaller_hour` and tiny's own `c-morning` tie (bins 3 and 10). Candidate: a `swap-order-key` operator over one `over (… order by a, b)` clause. | Phase 5 | A survivor class needs it, or a second window-function tie-break lands (Phase 6/7) |
| **The cohort-moment argmax ranges over opened bins, not all 24 hours** — an optimal window can always start at an opened bin, so the mass is right; but with `window_bins > 1` an EMPTY earlier start hour can tie the winning opened one and "smallest hour" would pick it while the code picks the opened one. Unreachable today (`window_minutes` 60 → `window_bins` 1 on every profile); records say "smallest opened hour". A 24-row start-hour generator is where a dialect form (`range` / `generate_array`) would first enter a model. *Phase 9b revisit: no profile with `window_minutes > 60`; the 24-row start-hour generator (`range` / `generate_array`) would be a dialect form behind a macro question — still hypothetical; unchanged.* | Review round 1, Phase 5 | A profile ships `window_minutes > 60`: scan all 24 start hours (a macro question) or pin the opened-bin tie-break as intended |
| **`ontime_retention` has no frozen golden** — on tiny (7 days) every `retained` is NULL by design, so a frozen file would pin nothing; the three states are proven by a synthetic unit test only. *Phase 5 revisit: `medium` (30 days) closes every window — 2,000 non-NULL `retained` — but runs unfrozen (Phase 5 item 1), so the trigger is not pulled; re-deferred unchanged.* | Phase 4 | First frozen profile whose window closes (≥ `retention_days` = 28 days of events — `medium` or a retention-shaped profile): freeze `expected/ontime_retention.csv` through the same `Golden` path |
| ~~**Model dialect denylist is non-exhaustive** — `tests/test_dbt_conventions.py::test_no_dialect_function_in_any_model` catches six known DuckDB/Postgres forms (`bool_or`, `logical_or`, `date_diff`, `timezone(`, `->>`, `::`); `epoch(`, `strftime`, `string_agg`, `try_cast` and any future inline form false-pass. The complete check is a build under the second dialect. *Phase 9a revisit: no BigQuery build ran; re-deferred to 9b unchanged.*~~ **DONE Phase 9b** — the BigQuery build is the complete check (`make dbt-build TARGET=bigquery`, `make test-int-bigquery`); the offline denylist stays as the early warning. Every inline form the first live build rejects joins it (none expected: the models were written ANSI on purpose — recorded in the 9b Evidence when the live build runs). | Review round 2, Phase 3 | Phase 9b (first `make dbt-build TARGET=bigquery`: any inline form fails to compile there; extend the list with each one found, or replace the test with the BigQuery build) |
| **Cross-warehouse dialect drift is caught only on DuckDB in CI** — BigQuery runs are manual. A scheduled or on-demand BigQuery CI job needs WIF; deferred until Phase 9 proves the five macros by hand. *Phase 9a revisit: the WIF pool/provider exist behind `enable_ci_wif` (default false) with no default `github_repository` (Amendments H, K); the CI job needs an explicit opt-in apply — 9b's step. Re-deferred to 9b exit.* *Phase 9b revisit: the parity run exists (`make test-int-bigquery`, laptop, as the SA) and CI has the offline suite; the CI leg needs the opt-in `enable_ci_wif = true` + `github_repository` apply (a runbook step in `docs/DEPLOYMENT.md`) and a `workflow_dispatch`-only job — not built in 9b (reconciliation item 4, choice (b): an unrun job is a claim, and the SA-id reservation makes a 9b apply a detour). Re-deferred with a dated trigger.* *Phase 12: RE-DEFERRED. Orthogonal to the live run — the laptop `test-int-bigquery`/`test-int-spanner` runs are the parity proof; the CI WIF leg is its own opt-in apply, not part of the demo. Trigger unchanged.* *Phase 13: RE-STATED (docs phase, no infra). The parity job is a code/infra change, out of a docs-only phase's Scope; it stays its own opt-in branch. Trigger unchanged.* *fix/roadmap 2026-09-01: the ~2026-09-28 SA-id date dropped from the trigger — that soft-delete window closed at Phase 9b when the SA was imported; otherwise unchanged.* | Phase 0 | The first `enable_ci_wif = true` apply (`docs/ROADMAP.md` item 8): add the `workflow_dispatch` `bigquery-parity` job on `google-github-actions/auth` + the `workload_identity_provider` output, run it once, then strike |
| **`THROUGH` is validated by shape, not calendar** — `landing/cli.py::THROUGH_RE` = `^\d{4}-\d{2}-\d{2}$`, so `9999-99-99` / `2026-13-40` pass. Harmless today: `THROUGH` is only string-compared to `events_*.jsonl` file names (`_file_date(f.name) <= through`), never a path or a SQL predicate — an impossible date simply matches no file. Flagged in review round 1, Phase 7 (security-reviewer); accepted with a trigger. *Phase 8b revisit: the DAG now feeds `THROUGH` from `{{ data_interval_end \| ds }}`, but it still only string-compares to file names — never a path or a query — so the trigger is not pulled; re-deferred unchanged. Phase 9a revisit: no landing or query changed (Terraform only); the trigger is 9b's `bq load` landing — re-deferred unchanged.* *Phase 9b revisit: the `bq-load` landing applies `THROUGH` the same way — a string compare over fixture file names (`landing.load.event_files`); the GCS object names come from the glob, never from `THROUGH` — so it still reaches no path and no query; re-deferred unchanged.* | Review round 1, Phase 7 | `THROUGH` ever reaches a path or a query (e.g. the Phase 9 BigQuery landing, or a `THROUGH` used in a SQL predicate): add calendar validation (`datetime.strptime`) or a date-typed bind |
| ~~**Round tags are phase-agnostic (`review-round-N`), so each phase's tags collide with the prior phase's leftovers** — Phase 0's `review-round-1`/`-2` tripped Phase 1's round-1 collision check; deleted by hand to proceed.~~ **DONE — `fix/round-tag-phase-reset`** — `round_tag.py reset` (+ `make round-reset`) deletes local `review-round-*`; the CLAUDE.md phase-start step runs it so a new phase's round 1 cannot collide. | Review round 1 | Phase 2 review round 1, or when the fix branch lands (whichever first) |
| ~~**`model_version` compares as a string** — `serving/writeback.py::should_replace` orders `(model_version, computed_as_of)` by Python tuple comparison, so `model_version` sorts lexically (`'v10' < 'v2'`). Harmless today: `v1` is the only version, so `computed_as_of` is the live discriminator and no two versions coexist. §2.9's "strictly greater `(model_version, computed_as_of)`" is honoured for the single version. *Phase 8b revisit: still `v1` only; re-checked, re-deferred unchanged.*~~ **DONE Phase 10** — no bump, but the phase's Done-when ("an older `model_version` never overwrites a newer one") is a for-all claim lexical order falsifies at `v10` vs `v2`, so the comparator was fixed before a Spanner table could ever hold a mis-ordered version: `serving/writeback.py::version_key` parses `v<int>` (any other shape refuses loudly — never a lexical fallback); `tests/test_writeback.py::test_version_orders_numerically_v10_beats_v2`, `…::test_malformed_version_refuses`. Contract wording unchanged. | Phase 8a | Phase 10's first `model_version` bump (or any second version): order by a parsed version (`(major, minor)`) or a monotonic integer before two versions can coexist |
| ~~**The DAG's build owns its landing in one task; load and `dbt build` are not split** — Phase 8b's DAG runs `make dbt-build … THROUGH=<interval>` (which lands ≤ THROUGH then builds) rather than a separate `load` task + a build-only target. Option A keeps landing and build in one owner per run, which is all 8b needs (no clobber, no interval built against a landing it did not load). The split earns its keep only at the BigQuery target, where the load is GCS→BigQuery and `dbt_build(TARGET=bigquery)` calling the DuckDB `load()` (`loader/cli.py`) is already wrong. *Phase 9a revisit: 9a touched no DAG; its one loader change (Amendment S) refuses `TARGET=bigquery` before `load()` — the build-only target is still absent; the trigger is 9b's reconciliation (its spec is finalized after 9a merges). Re-deferred unchanged.*~~ **DONE Phase 9b** — `loader/cli.py::land` dispatches the landing by target (`duckdb` → `load()`, `bigquery` → `bq_load()`, never the other — `tests/test_loader.py::test_bigquery_build_lands_through_bq_not_duckdb`); `make bq-load` is the standalone landing; `orchestration/tasks.py` carries `TARGET` as a literal. The DAG stays two tasks (a third buys nothing before Composer); a Composer-era split is Phase 11's row if it needs one. | Phase 8b (Amendment 1) | Phase 9b spec reconciliation, or the first `TARGET≠duckdb` DAG: add a build-only target + a separate `load` task, and thread `TARGET` through the DAG |
| ~~**9b's dbt build must land inside the two Terraform datasets** — `dbt/dbt_project.yml` sets a per-folder `+schema` (`staging`, `attribution`, `marts`, `features`, `scores`) and has no `generate_schema_name`, so dbt's default on the `bigquery` target would create `ontime_staging … ontime_scores` — five datasets (in the US multi-region, not `us-central1`) that Terraform never creates and `tf-destroy` never removes, falsifying 9a's Done-when 5 ("none created out of band"). The SA's dataset-scoped `dataEditor` cannot create a dataset (`tests/test_infra.py::test_no_role_can_create_a_dataset`), so a build as the SA would fail rather than sprawl — but an operator's Owner ADC CAN create them (Amendment N: no BigQuery build before 9b; 9b's manual builds impersonate the SA) — and it must be made to land. Second clause: `test-int-bigquery` in CI needs an explicit `enable_ci_wif = true` + `github_repository` apply (a default apply builds no WIF — H, K). Third and fourth (Amendment O): `dbt/profiles.yml`'s `bigquery` output sets no `location` (dbt-bigquery defaults to the US multi-region; the datasets are `us-central1` → "Dataset … not found in location US") and reads `OTR_GCP_PROJECT`, which nothing sets or documents.~~ **DONE Phase 9b** — `dbt/macros/generate_schema_name.sql` collapses to `target.schema` (`ontime`) on `target.type == 'bigquery'` only (DuckDB keeps `main_<folder>`); `profiles.yml` `location: us-central1`; `loader/cli.py` exports `OTR_GCP_PROJECT` from the validated `PROJECT` in-process and lifted Amendment S in the same commit; the CI WIF apply is a DEPLOYMENT runbook step (the CI job itself: the re-deferred drift row). Live `bq ls` = two datasets is `test-int-bigquery::test_exactly_two_datasets_exist`. | Review round 4, Phase 9a (Amendment I); round 5 (N, O) | **DUE** Phase 9b spec reconciliation: add `generate_schema_name` (BigQuery → `models_dataset`; whether DuckDB keeps per-folder schemas or collapses too is 9b's call — collapsing touches every schema-qualified reader in `serving/`, `eval/`, tests); name the opt-in WIF apply as a step; set `location: us-central1` from Terraform's value; have `make dbt-build TARGET=bigquery` export `OTR_GCP_PROJECT` from the validated `PROJECT` (empty = refusal); lift `loader/cli.py`'s unconditional pre-9b `TARGET=bigquery` refusal (Amendment S) in the SAME commit that adds `generate_schema_name` |
| **`computed_as_of` is not a complete discriminator for a served-row change** — `serving/writeback.py::should_replace` compares `(model_version, computed_as_of)` where `computed_as_of = max(client_event_time)` of the feature window's opens (`scores_send_time.sql`). It catches a change only when that max advances; a re-run/backfill that alters the served row without advancing it keeps the stale serving row → backfill ≠ union. Two cases: (a) scores change via **only back-dated opens** (or a max receding as opens leave the 30-day window); (b) **`tz` changes** — `tz` is a served `send_schedule` column sourced from `dim_user_current`, so an SCD2 `dim_user` change alters the served row while `scores_send_time`/`computed_as_of` are untouched. Both unreachable on tiny/medium as run: monotone-superset landings add forward-dated opens (verified — `THROUGH=2026-01-12` scores already equal the union), and `load_dims` is never `THROUGH`-filtered so `tz` is constant across a backfill. The 8b `backfill ≡ union` invariant is stated with this precondition (Amendment 2). *Phase 10 revisit (reconciliation item 2): NOT pulled — the phase bumps nothing, no second version coexists, and no score changes without an advancing `computed_as_of`; Phase 10's `model_version` work was the comparator (its own row, struck), not a second version. Replacing the discriminator is a design change to the 8a contract Phase 10 was told to keep. Re-deferred.* | Phase 8b review rounds 1/5 (#4/#2); the code is 8a/Phase-5 | A profile/backfill that changes a served row without advancing `computed_as_of`, a `tz`/dim change landing mid-schedule, or a second `model_version` coexisting live: replace the discriminator with a content hash of the served columns (or a monotonic row version) — a fix PR against the 8a write-back contract |
| **The offline DAG structure test cannot pin DAG↔task attachment** — `tests/test_dag_structure.py` loads `pipeline_dag.py` with a stubbed airflow and asserts config, the two operators, and edge direction, but the stub does not model Airflow's DAG↔task registration, so an operator built OUTSIDE `with DAG(...)` (mutation M12) survives offline. The container `test_int_airflow.py::test_dag_edges_and_operators` IS the killer (M12 fails DAG import there: "Tried to create relationships between tasks that don't have DAGs yet"), but `test-int-airflow` is behind `OTR_INT` and CI has no Docker. Accepted residual, documented in the stub's docstring. *Phase 11 revisit: the Composer module changed no DAG code (`pipeline_dag.py` is uploaded verbatim) and added no Docker to CI, so neither trigger leg is pulled by a plan-only infra phase; re-deferred unchanged — the container test is the killer, so the natural home is Phase 12 (the live Composer run) or a CI-Docker change.* *Phase 12: RE-DEFERRED. Phase 12 adds no Docker to CI; `test_int_airflow.py::test_dag_edges_and_operators` IS the killer and the demo-day rehearsal runs it against real Airflow. Phase 12 edits `tasks.py` (the cloud-target render) but the offline stub still cannot model DAG↔task registration — residual unchanged. Trigger unchanged: CI gains Docker.* | Phase 8b review round 5 (#1) | CI gains Docker (run `test-int-airflow` in CI — the attachment pin then runs everywhere), or Phase 12 (live Composer run): make the offline stub track a current-dag via `__enter__`/`__exit__` + a `dag=` kwarg, or rely on the now-in-CI container test |
| ~~**`ontime-pipeline` SA id is soft-deleted on `<project_id>` until ~2026-09-28** — the Evidence-row-5 destroy ran 2026-08-29; GCP reserves a deleted SA's id for 30 days, so a 9b apply on that project before then fails "already exists, in a deleted state" (`docs/DEPLOYMENT.md` gotcha; ARCHITECTURE §8). No code change — a dated operator constraint.~~ **DONE Phase 9b** — the first 9b apply (2026-08-30) ran the detour: `gcloud iam service-accounts undelete <unique_id>` + `terraform import module.iam.google_service_account.pipeline …` (state is empty after a destroy), then `Apply complete! 18 added` (`docs/DEPLOYMENT.md`). | Review round 6, Phase 9a (#20) | 9b's first `tf-apply` on that project: before 2026-09-28 run `gcloud iam service-accounts undelete` first; after it, strike this row |
| **The conflicting-duplicate dbt test is weaker than the landing's Python predicate by contract** — `dbt/tests/assert_no_conflicting_duplicates.sql` compares the payload key by key through `json_extract` (a JSON column is neither groupable nor castable on BigQuery — §8), marking nulls explicitly; but a JSON null and a MISSING key both read as null, and a `\|` inside a value could alias the join. Unreachable today: `generator/models.py::PROPERTY_KEYS` fixes the exact key set per event_type (null and missing cannot coexist for one type) and every value is a counter or an error code. `landing/load.py::conflicting_duplicates` (DuckDB) compares the whole payload. | Review round 1, Phase 9b (#3/#4) | An optional key or a free-text property value enters the contract: hash the payload per key with a length-prefixed encoding, or land BigQuery through the Python predicate too |
| ~~**`TF_VAR_*` from the environment bypasses Amendment T** — `infra/cli.py` refuses an auto-loaded `terraform.tfvars`/`*.auto.tfvars*` but reads no `TF_VAR_*`; an exported `TF_VAR_enable_composer=true` lets a plain `make tf-apply … CONFIRM=yes` create billable resources with nothing in the argv (9a's DEPLOYMENT already said "or `TF_VAR_*`"; 9b's build-as-SA runbook uses the inline `TF_VAR_x=… make …` form, which the command line shows). A 9a residual: a needed change in an earlier phase is its own fix PR.~~ **DONE — `fix/tf-vars-argv`** — `infra/cli.py` refuses plan/apply/destroy while any `TF_VAR_*` is in its environment and takes toggles only as `VARS='name=value,…'` → argv `-var` (validated; `project_id` excluded); `tests/test_infra.py::test_cli_vars_are_the_only_toggle_path`, `tests/test_makefile.py::test_tf_targets_pass_vars_as_one_literal`; DEPLOYMENT reworded. | Review round 1, Phase 9b (#9; security-reviewer) | `fix/tf-vars-argv` after 9b merges: `infra/cli.py` refuses any `TF_VAR_*` in its environment and takes toggles only through an explicit `VARS='k=v,…'` make variable → argv `-var` (validated, `_Q`-quoted); DEPLOYMENT reworded |
| ~~**The live project id and the derived SA email sit in tracked records** — `<project_id>` / `ontime-pipeline@…` appear in DEPLOYMENT, the 9a/9b specs' Evidence and this file (the operator address is redacted). Informational, not a credential: the project is the named demo project and 9a's records already carry it. *Phase 10 review round 2 (#4): the accepted set is the project id, the SA email, the project NUMBER (in error text) and the SHAPE of the SA's numeric unique id — the id itself is read from the local gitignored state backup at detour time and is redacted to `<sa-unique-id>` in the spec; billing-account and budget ids are likewise never recorded.*~~ **DONE — `fix/public-release`** (2026-09-01, the pre-publication security review): every record now reads `<project_id>` / `<operator>` (DEPLOYMENT, PHASES, RESULTS, the 9a–12 specs, this file); the tests keep the project-id-SHAPED literal, which is the repo's own name; the id in git history is accepted in DECISIONS (fix/public-release), not rewritten. | Review round 2, Phase 9b (#19; security-reviewer note) | The repo goes public or the project is reused: redact to `<project_id>` in every record, or accept explicitly in DECISIONS |
| ~~**The write-back reads DuckDB relation names on a DuckDB connection only** — `serving/writeback.py` hard-codes `main_scores.scores_send_time` / `main_marts.dim_user_current` and opens `data/<p>.duckdb`; on BigQuery the same models are `<project>.ontime.scores_send_time` / `….dim_user_current` (9b's `generate_schema_name`), and `eval/golden.py::select_sql` already grew a relation override for the parity read. Phase 10's `make writeback TARGET=spanner` (PHASES) needs a warehouse seam on the READ side too (target → connection + relation names), not only the Spanner write target. 9b's models exist on both warehouses; the seam is not 9b's scope.~~ **DONE Phase 10** — `serving/writeback.py::candidates_sql(scores, dims)` is the one read with a Golden-style relation override; `serving/spanner.py::relations` supplies `<project>.ontime.*` and `TARGET=spanner` reads BigQuery / writes Spanner through injectable clients (one knob, two named configurations); `tests/test_writeback.py::test_reader_relations_per_target`. | Phase 9b exit audit (#4) | Phase 10 spec reconciliation: a `TARGET`-keyed read seam in `serving/` (the `Golden`-style relation override) beside the Spanner write target; `test-int-bigquery`'s read helper is the shape |
| ~~**`loader/` holds more than the landing** — the DuckDB landing (`load.py`), the GCS→BigQuery landing (`bq.py`), the build dispatcher (`cli.py::dbt_build`) and the integration launcher (`cli.py::int_bigquery`) share one package because every `make` target needs a validated, `$(origin)`-gated Python entry. The two landings belong together (one contract, two engines); the dispatcher and the launcher are pipeline plumbing, not landing. *Phase 10 revisit (reconciliation item 3): a separate fix branch, not this phase — the rename is mechanical, zero-behaviour and record-heavy, and folding it into the Spanner phase doubles the review surface. Phase 10 added the third landing engine (`loader/spanner.py`) and the `test-int-spanner` launcher to the same package; both move with the rename.*~~ **DONE `fix/landing-package`** (2026-08-31) — `loader/` renamed to `landing/` (the two landings, the Spanner dims landing, and `land` — the TARGET dispatcher both CLIs share); `dbt_build`, `int_bigquery`, `int_spanner`, `full_refresh_args`, `dbt_vars_args` moved to the new `pipeline/cli.py`, which imports the validators and `land` from `landing.cli`. Zero behaviour: every `make` target NAME is unchanged, only the `-m` module path differs (`load`/`bq-load`/`spanner-load`/`drop-db` → `landing.cli`; `dbt-build`/`test-int-*` → `pipeline.cli`). `tests/test_loader.py` → `tests/test_landing.py`; the truth-isolation guard (tree-derived) covers both new packages automatically. | Phase 9b exit (architect's question 2) | `fix/landing-package`, branched after Phase 10 merges and before Phase 11: rename the package `landing/` and move `dbt_build` + the `int_*` launchers to `pipeline/cli.py` beside `serving/cli.py::pipeline`; the truth-isolation guard follows the rename automatically |
| **The cloud-env redirection gate is not fully closed** — Amendment Q refuses the transport-redirection class on every cloud path via `infra.cli.REDIRECTION_NAMES`, but three residuals remain: (1) the PROXY sub-class is enumerated by spelling, so mixed-case `<scheme>_proxy` variants `requests`/`urllib` honor still pass (`grpc_proxy` was added; the general fix is the casefold-`_proxy` predicate the libraries use); (2) `REDIRECTION_NAMES`/`CLOUD_ENV_NAMES` carry no exact `==` membership pin, so a dropped name passes the suite silently; (3) no test drives a redirection name through the IN-PROCESS entry points (`bq-load`, `writeback TARGET=spanner`) — only transitively. Accepted (the gate REFUSES, not scrubs; the residual is exotic proxy spellings on a local operator env, not a remote path). *Phase 11 revisit: the Composer module made NO cloud-env gate change — no new cloud command, no `infra/cli.py` env edit (the `enable_composer` toggle reaches Terraform through the existing `VARS='name=value,…'` → argv `-var` path; DAG upload is a Terraform resource on `tf-apply`, not a new target). Folding the close into a plan-only infra phase is unearned scope; re-deferred with a sharper trigger.* *Phase 12: RE-DEFERRED. The live Composer apply uses the existing `tf-apply … VARS='enable_composer=true'` path (gated by the existing `CLOUD_ENV_ALLOW`); Phase 12 makes no `infra/cli.py` cloud-env / `ENV_ALLOW` edit, and the residual (exotic proxy spellings on a local operator env; the gate REFUSES, never scrubs) is not on the demo path. Trigger sharpened: the next `infra/cli.py` cloud-env or `ENV_ALLOW` edit.* | Phase 10 security re-review of P (code-reviewer #1/#2, security-reviewer #1, functionality-tester coverage gap) | The next edit to `infra/cli.py`'s cloud-env or `ENV_ALLOW` allowlist, or Phase 12's live Composer command surface (whichever first): replace the proxy half of `REDIRECTION_NAMES` with a `name.casefold().endswith("_proxy")` predicate in `in_cloud_namespace`, add `REDIRECTION_NAMES`/`CLOUD_ENV_NAMES == frozenset({…})` pins beside the prefix/suffix/allow pins, and add a `REDIRECTION_NAMES` member to `test_every_cloud_command_refuses_a_credential_in_the_env` |
| **`APPDATA` is refused as identity, contradicting P4's acceptance of its siblings** — `infra.cli.CLOUD_ENV_NAMES` refuses `APPDATA` (the Windows ADC config root), but P4 ACCEPTS `CLOUDSDK_CONFIG`/`HOME` on "ADC must live somewhere"; `APPDATA` is always set on Windows and is that same root, so refusing it would hard-block every cloud command on a Windows runbook. Inert today (macOS / uv only). | Phase 10 security re-review of P (security-reviewer note, code-reviewer suggestion) | A Windows runbook is ever supported: accept `APPDATA` with the P4 rationale (or move it to `ENV_ALLOW`'s spirit), or keep refusing and drop the parity claim |
| ~~**`docs/PHASES.md`'s Phase 10 "Delivered" narrative stops at N1–N3 ("ten amendments")** — it does not mention rounds 5 (O), 6 (P) or the security re-review (Q). Completed-phase history; no Done-when clause is falsified (DECISIONS, the spec and CLAUDE status are complete).~~ **DONE Phase 11** — the Phase 10 Delivered narrative in `docs/PHASES.md` now appends O/P/Q and the security re-review, and the amendment count is corrected. | Phase 10 exit coherence audit (note) | Phase 11 doc pass, or the next `docs/PHASES.md` edit: append O/P/Q to the Phase 10 Delivered narrative and correct the amendment count |
| ~~**The Composer DAG upload is a flat `dags/` layout the 8b DAG's import does not resolve** — Phase 11's module uploads `dags/pipeline_dag.py` + `dags/tasks.py` as two flat `google_storage_bucket_object`s, but `orchestration/dags/pipeline_dag.py` does `from orchestration.tasks import TASKS` — a package path that will not resolve in a Composer DAG bucket (only `dags/` is on `sys.path`, so `import tasks` would work but `orchestration.tasks` will not).~~ **DONE Phase 12** — the DAG import is dual-path: `try: from orchestration.tasks import TASKS except ImportError: from tasks import TASKS`, resolving the package path offline/in Docker and the flat `import tasks` in the Composer bucket. Proven by `tests/test_dag_structure.py::test_dag_imports_in_flat_bucket_layout` (imports the DAG with only a flat `dags/` dir on `sys.path`, `orchestration` blocked). | Phase 11 exit coherence audit (note) + functionality-tester | Phase 12 (live Composer run), before the first DAG parse: ship the `orchestration` package layout to the DAG bucket (or a `plugins/` package), or adjust the DAG's import to the flat `import tasks`, and prove it parses in a real environment |
| ~~**`test_conftest_scrub_uses_the_cloud_env_policy` is brittle under `FORCE_COLOR`** — the test spawns a child pytest and asserts `"::test_probe" in bare_out` over the child's `stdout[-2000:]`; when pytest colorizes output (the Claude Code harness exports `FORCE_COLOR`), the short-summary `FAILED …::test_probe` line is ANSI-wrapped/absent in the tail, so the assertion fails even though the scrub is correct (`unlisted_cloud_env() == []`, and the same suite is 585-green with `FORCE_COLOR` unset — as CI runs). Found during Phase 12 review prep under the harness; OUT of Phase 12 scope (the phase touched no scrub / `conftest.py` / `infra.cli` code — a `make review-gate` under the harness surfaced it).~~ **DONE — `fix/force-color-scrub-test`** (2026-08-31) — the child pytest now runs with `--color=no`, so the child emits plain output no matter what colour env it inherited through `**os.environ` (the harness's `FORCE_COLOR`); the `::test_probe` node id and the `unlisted_cloud_env` traceback text stay contiguous in the tail. Verified green both with `FORCE_COLOR=1` and with it unset. | Phase 12 review prep (2026-09-01) | The next edit to the scrub test or `conftest.py`'s scrub, or a colorized CI: make the assertion colour-agnostic — pass `--color=no` to the child pytest, or strip ANSI before the substring check, or assert on the child returncode + a plain-text marker |
| **The make-based DAG cannot run on Composer (Option A leaves the scheduled cloud run unproven)** — Phase 12 proved the committed DAG *parses and registers* on managed Airflow, but its `BashOperator`s shell out to `make` with `cwd=/home/airflow`, where a Composer worker has no repo / `make` / dbt venv, so the task fails there by design (ARCHITECTURE §8; the green data run is the local Docker-Airflow rehearsal). Making it truly execute on Composer means a different task model — Cosmos (dbt-in-Airflow) or a `KubernetesPodOperator` running a repo/toolchain image — which SUPERSEDES Option A. Named as its own future branch; NOT built in Phase 13 (a docs phase). | Phase 12 exit (Option A); re-stated Phase 13 | A scheduled cloud DAG run (not a demo) is wanted: replace the `make`-shelling `BashOperator`s with Cosmos or a `KubernetesPodOperator` over a baked repo/toolchain image, and prove one green scheduled run on Composer |
| **No real-scale cost/performance numbers are published** — the served numbers are `tiny` (frozen) and `medium` (2,000 users, unfrozen, DuckDB); no large profile has been built on BigQuery to report real query cost, slot time, or Spanner write throughput. The README/INSIGHT deliberately claim only what is proven. | Phase 13 (docs capstone) | A decision to publish real-scale cost numbers: define a large profile, run `make dbt-build TARGET=bigquery` on it ask-first, and record cost/latency in `docs/RESULTS.md` (its own branch, cloud-cost) |
| ~~**The findings chart and README table hardcode structural labels that are not pin-derived** — `eval/readme.py` embeds `"(medium, 60,000 prompts)"` (`render_svg`), `"2,000 users"` and `"20-user cohort"` (`render_block`) as string literals. They are labels, not the metric figures the "no number typed by hand" invariant enumerates (accuracy/MAE/coverage/lift/on-time), so they do not violate it — but no test pins them to the `medium` profile's shape, so they go stale silently if it changes.~~ **DONE — `fix/front-door`** (2026-09-01): `first_screen_rows` carries `users_tiny` (`SCORES_ROWS`), `users_medium` (`MEDIUM_USERS`) and `prompts_medium` (`PROMPTS_SENT_MEDIUM`); the block, its headline and the chart title render from them; `tests/test_readme.py::test_structural_labels_derive_from_pins` asserts no literal is left in either renderer. | Phase 13 exit coherence audit (note) | The `medium` profile's user count or prompt volume changes, or the next `eval/readme.py` edit: derive the labels from the profile/pins, or add a test pinning them to the profile shape |
| ~~**`docs/INSIGHT.md` hand-types pinned figures with no value-parity test** — the essay types `+0.162371`, `0.816201 h`, `0.352354 h`, `0.460920 → 0.623291`, `1.000` by hand (link-checked, not pinned — DECISIONS frames INSIGHT as the author's essay). They match the pins today, but the `medium` numbers are unfrozen/regenerated, so INSIGHT can drift silently if a medium pin moves; only `check-docs` link/trace covers it, not value parity. *fix/front-door (2026-09-01): the README's caveat paragraph is a third hand-maintained copy of the same narrative (README prose, INSIGHT, the RESULTS preamble) — number-free by design, so only the wording can drift; same trigger.*~~ **DONE — `fix/process-doc`** (2026-09-01): `tests/test_insight.py` asserts every typed figure (the medium lift and its two rates, tiny's lift, both MAEs, the accuracy, the three structural counts) equals the pin it renders from, that the six-decimal figures in the page are EXACTLY the pinned set (a new hand-typed number is red until pinned), and that a moved pin is caught; the README caveat paragraph stays number-free by design. | Phase 13 exit coherence audit (note) | The next `docs/INSIGHT.md` edit, or a `medium` pin move: add a value-parity check (assert the typed figures equal the pins) or regenerate the essay's figures through a marker block |
| ~~**README and INSIGHT prose still read as a phase log** — scope, mechanism and proof live in [docs/ROADMAP.md](docs/ROADMAP.md) item 1 only (a roadmap row here carries no detail: two copies drifted twice in one review round). *fix/front-door (2026-09-01) landed the README half (ROADMAP item 1a: the story, no phase language, the headline generated); re-deferred: `docs/PROCESS.md` and the INSIGHT prose pass are item 1b, branch `fix/process-doc`.*~~ **DONE — `fix/process-doc`** (2026-09-01, ROADMAP item 1b): `docs/PROCESS.md` landed (the loop, the three offline commands, what holds the data still, the reviewers and the two rules the rounds taught, the assistant's part, the trail); INSIGHT's closing paragraph now reads as a product statement and points at PROCESS; the README's "How it was built" and docs index point at the page. | Post-13 staff-DE read (ROADMAP item 1) | `fix/front-door`, first in the one-week cut |
| **`scores_send_time` reads the raw `dim_user` source for the open row while `dim_user_current` exists for exactly that** — detail in [docs/ROADMAP.md](docs/ROADMAP.md) item 3 only. | Post-13 staff-DE read (ROADMAP item 3) | `fix/scores-dim-current`, in the one-week cut |
| **The only offline evaluation of the served schedule is the circular simulation** — detail (the upload-date cut, raw-only scoring, the amendment) in [docs/ROADMAP.md](docs/ROADMAP.md) item 4 only. | Post-13 staff-DE read (ROADMAP item 4) | `fix/holdout-eval`, after the one-week cut |
| **The landing is a full `WRITE_TRUNCATE` re-land while every downstream model is incremental** — detail (append-only partitioned raw, the spec + re-freeze) in [docs/ROADMAP.md](docs/ROADMAP.md) item 6 only. | Post-13 staff-DE read (ROADMAP item 6) | `fix/append-landing`, after item 5 |
| **The public-repo GitHub-side settings are outside the tree** — Actions → "Require approval for all external contributors" (the API refuses it on a private repo — verified 2026-09-01; CI runs `on: pull_request` with a read-only token, no secrets, SHA-pinned actions, so until it is gated a fork PR runs its own Makefile on the runner), and the CI-WIF layer's `github_repository` must be the PUBLIC `<owner>/<repo>` slug when `enable_ci_wif=true` is first applied (ROADMAP item 8). Neither lives in a file the repo tracks. Also before the flip: delete the merged `origin` branches (24 today — every phase branch and `fix/force-color-scrub-test`); their tips hold pre-redaction records, and the squash-merged history on `main` is the accepted copy (DECISIONS, fix/public-release). *2026-09-01, flipped: the 23 merged origin branches deleted (each verified MERGED, 0 commits outside `main`), visibility set to public, then the Actions approval policy set to all external contributors and read back the same minute. Remaining: the WIF slug.* | fix/public-release exit (2026-09-01; coherence-auditor #6) | The first `enable_ci_wif=true` apply passes the public slug |
| **Nothing executes the README quickstart in CI** — `tests/test_readme.py::test_quickstart_commands_are_cloud_free` checks tokens and target names; the eight-command chain running green on a cold clone was proven by hand at the `fix/front-door` exit (every OK line reproduced, the re-run write-back wrote 0) and by nothing automated. | fix/front-door exit (2026-09-01; functionality-tester gap #2) | The quickstart fence changes, or a cold-clone report of a failing step: add an `OTR_INT`-gated `tests/integration/test_quickstart.py` running the fence's `make` lines in a scratch `data/` and asserting each OK line (out of `make test` — a dbt build) |
